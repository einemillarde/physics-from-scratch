# Physics Engine From Scratch (Rust + Wgpu)

Note: Right now none of the physics are implemented, so the src/physics_calc.rs module and anything in the src/physics_calc/ directory is unused and most likely broken.
